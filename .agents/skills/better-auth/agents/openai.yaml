interface:
  display_name: "Better Auth"
  short_description: "Repo-specific patterns and workflows for better-auth"
  default_prompt: "Use the better-auth repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true